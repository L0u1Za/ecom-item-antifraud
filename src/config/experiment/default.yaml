name: "baseline"
tags: ["fraud-detection", "multimodal"]

wandb:
  enabled: true
  project: "fraud-detection"
  entity: "luizanigogosova"
  
data:
  train_path: "data/train.csv"
  val_path: "data/val.csv"
  test_path: "data/test.csv"
  train_images_path: "data/train_images/train_images"
  test_images_path: "data/test_images/test_images"
  num_workers: 4
  
checkpointing:
  dir: "data/checkpoints"
  save_best: true
  save_last: true
  save_frequency: 10  # Save every N epochs
  save_frequency_enabled: true  # Enable periodic saving